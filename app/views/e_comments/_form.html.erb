<% e_comment = EComment.new %>

<%= form_with(model: [@event, e_comment]) do |form| %>
  <% if e_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(e_comment.errors.count, "error") %> prohibited this e_comment from being saved:</h2>

      <ul>
        <% e_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>