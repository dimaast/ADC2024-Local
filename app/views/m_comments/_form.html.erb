<% m_comment = MComment.new %>

<%= form_with(model: [@meet, m_comment]) do |form| %>
  <% if m_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(m_comment.errors.count, "error") %> prohibited this m_comment from being saved:</h2>

      <ul>
        <% m_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
