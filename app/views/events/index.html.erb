<p style="color: green"><%= notice %></p>

<% content_for :title, "Events" %>

<h1 class = "page_title">Events</h1>

<%= link_to "New event", new_event_path %>

<div id="events">
  <% @events.each do |event| %>
    <%= link_to event, class: 'event_card' do %>
      <%= render event %>
    <% end %>
  <% end %>
</div>